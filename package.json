{
  "name": "thats-my-best-engineering-case-study",
  "version": "1.0.0",
  "private": true,
  "description": "A sanitized reference model for a multimodal social quiz system whose answer key is model-proposed by default and bounded by machine-checkable invariants.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/reference-model.mjs && node --test"
  },
  "author": "Alex Bouchard",
  "license": "MIT"
}

